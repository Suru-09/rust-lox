{
  "name": "gh_pages",
  "version": "1.0.0",
  "description": "Web UI for rlox interpreter",
  "dependencies": {},
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "cd rlox-web/ && npm install && npm run buildWasm"
  },
  "author": "",
  "license": "MIT"
}